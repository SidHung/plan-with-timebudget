{
  "name": "plan-with-timebudget",
  "version": "0.3.1",
  "private": true,
  "scripts": {
    "test": "python3 -m unittest discover -s tests -p 'test_*.py' && node --test tests/test_interactive_plan.mjs",
    "test:contract": "python3 -m unittest discover -s tests -p 'test_*.py'",
    "test:browser": "node --test tests/test_interactive_plan.mjs"
  }
}
